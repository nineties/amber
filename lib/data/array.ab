# Copyright (C) 2012 nineties
#
# $Id: array.ab 2012-11-23 12:42:25 nineties $

module std {
    Array{elems...} => `Array::from_list(!elems)

    each(ary@Array): {
        i: 0
        n: ary.size
        () -> {
            if (i >= n) return undefined
            v: ary[i]
            i += 1
            v
        }
    }
}
