# Copyright (C) 2014 nineties
# $Id: GC.ab 2015-06-06 17:36:06 nineties $

start() := prim.GC_start()

# return (used memsize, allocated memsize) in bytes
used()  := prim.GC_used()

addressof(obj) := prim.GC_addressof(obj)
