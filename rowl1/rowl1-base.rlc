;
; rowl - 1st generation
; Copyright (C) 2010 nineties
;
; $Id: rowl1-base.rlc 2012-05-17 19:26:02 nineties $
;

(import "rlvm-compile")
(import "rowl1-types")

(compile `object `(

(import "rowl1-util")
(import "rowl1-node")
(import "rowl1-compile")
(import "rowl1-assemble")

(fun _equal (a b) (
    (if (== a b)
        (return true)
        (return false)
        )
    ))

(export fun setup_base (mod) (
    (add_builtin_function2 mod (to_sym "equal") DontCare DontCare _equal 0)
    ))

    ))
