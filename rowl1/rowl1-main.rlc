;
; rowl - generation 1
; Copyright (C) 2010 nineties
;
; $Id: rowl1-main.rlc 2010-10-19 15:30:52 nineties $
;

(import "rlvm-compile")
(import "rowl1-types")

(compile `object `(

(import "rowl1-parse")
(import "rowl1-node")

(fun main (argc argv) (
    (if (== argc 0) (do
        (output_string stderr "not input files\n")
        (exit 1)
        ))
    (for i 0 argc
        (pp_prog stdout (parse (array_get string argv i)))
        )
    (return 0)
    ))

    ))
